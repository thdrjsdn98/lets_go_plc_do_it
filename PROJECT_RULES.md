# 지멘스 PLC 기초강좌 - 작업 규칙

이 프로젝트를 수정하는 AI(어떤 모델이든)는 아래 규칙을 반드시 따라주세요.

## 0. 프로젝트 구조 (중요)
이 프로젝트는 **하나의 앱이 아니라, 주제별로 독립된 6개의 미니 웹앱 + 허브(root)**로 구성되어 있다.
(※ 과거 "전기기사 필기 요약집"이었던 프로젝트를 지멘스 PLC 기초강좌 전용으로 전면 개편한 것이며, 기존 전기기사 6과목 폴더는 모두 삭제했다.)
주제 폴더를 수정할 때는 **그 폴더 안의 index.html / sw.js만** 건드리면 되고, 다른 주제에는 영향을 주지 않는다.

| 폴더 | 주제 | sw.js의 CACHE_NAME 접두어 |
|---|---|---|
| `/` (루트) | 주제 선택 허브 | `plc-hub-cache-vX` |
| `plc-usage/` | ① PLC 사용법 (TIA Portal 시작하기·화면 구성) | `plc-usage-cache-vX` |
| `plc-shortcuts/` | ② PLC 단축키 | `plc-shortcuts-cache-vX` |
| `plc-hardware/` | ③ 핵심내용① 하드웨어·통신 설정 | `plc-hardware-cache-vX` |
| `plc-ladder/` | ④ 핵심내용② 래더(LAD) 기초 명령어 | `plc-ladder-cache-vX` |
| `plc-example1/` | ⑤ 예제① 자기유지회로·인터록 | `plc-example1-cache-vX` |
| `plc-example2/` | ⑥ 예제② 타이머·카운터 응용 | `plc-example2-cache-vX` |

### ✅ 해결된 이슈 (참고용 기록)
- ~~루트 `sw.js` 캐시 이름이 `circuit-theory/`와 겹치던 문제~~ → `electric-hub-cache-v1`로 분리 완료.
- ~~changelog 패널 없음~~ → 6개 과목 전부 표지에 업데이트 내역 패널 추가 완료.
- 버전 표기 방식은 폴더마다 다르다 (`v1`, `v2`, `v13` 등 혼재). 캐시 버전을 올릴 때는 **그 폴더에 기존에 쓰던 숫자 형식을 유지**하며 1씩 올린다.

## 1. 버전 관리
- 업데이트 규모에 따라 버전 자리수를 다르게 올린다:
  - **소규모 (+0.0.1)**: 오탈자·문구 수정, 스타일 미세 조정, 단일 버그 수정, 타임스탬프/캐시 동기화 등
  - **중규모 (+0.1.0)**: 새 기능 추가, 여러 UI 개선을 묶어서 적용, 여러 파일에 걸친 개선
  - **대규모 (+0.5.0)**: 콘텐츠 구조 재편성(단원 분할/재배치), 전체 디자인 톤 변경, 대규모 콘텐츠 추가
  - 자리올림은 일반적인 버전 표기 규칙을 따른다 (예: 0.0.9 다음 소규모 업데이트 → 0.1.0 등 자연스럽게 올림).
  - 애매한 경우 더 작은 쪽으로 판단하고, 애매하면 사용자에게 규모를 확인한다.
  - 수정한 과목 폴더의 `index.html` 안 `버전: VX.X.X` 문구를 위 기준에 맞게 올린다.
- 아직 changelog(업데이트 내역) 패널이 없는 과목이 대부분이다. 만약 소방안전관리자 프로젝트처럼 표지에 업데이트 내역 패널을 추가하고 싶다면, 사용자에게 먼저 확인 후 추가한다 (임의로 구조를 바꾸지 않는다).
- 여러 과목을 동시에 수정했다면, 수정한 과목들의 버전을 각각 개별적으로 올린다 (전체를 하나의 버전으로 묶지 않는다).

## 2. 서비스워커 캐시 버전 (매우 중요!)
- 과목 폴더의 `index.html`, `script.js`, `style.css`, `questions.js` 등 그 과목 안의 파일을 수정했다면, **그 과목의 `sw.js`에 있는 `CACHE_NAME`도 반드시 1씩 올린다.**
- 이걸 안 올리면 PWA로 설치한 사용자 브라우저가 예전 캐시된 파일을 계속 보여줄 수 있다.
- 건드리지 않은 다른 과목의 `sw.js`는 그대로 둔다.

## 3. 파일 구조 공통 사항
- 각 과목 폴더는 `index.html`(문제/이론 UI), `script.js`(로직), `style.css`, `questions.js`(문제 데이터), `sw.js`(캐시), `manifest.json`(PWA 설정)로 구성된다.
- 다이어그램이 있다면 순수 인라인 SVG 스타일을 유지한다 (외부 라이브러리 없이).
- 색상은 하드코딩하지 않고 `style.css`에 정의된 CSS 변수를 최대한 사용한다.

## 4. 마무리 시 항상 할 것
1. 수정 완료 후 프로젝트 폴더 전체(루트부터)를 다시 zip으로 압축한다.
2. 파일명 형식: `VX_X_X_영문요약.zip` (⚠️ 한글/괄호/공백 금지 — 실제로 모바일 다운로드 실패 사례 발생함)
   - 여러 과목을 수정했다면 요약에 과목명을 영문으로 포함한다. 예: `V1_0_2_circuit_power_typo_fix.zip`
   - 버전 번호와 요약 모두 밑줄(`_`)로 구분하고, 한글·괄호·공백은 절대 사용하지 않는다.
3. 사용자에게 파일을 제공(present)한다.

---
### 다른 채팅방/다른 AI로 옮길 때 사용법
이 zip 파일을 업로드한 뒤, 아래처럼 요청하면 규칙이 이어집니다:

> "압축 파일 안의 PROJECT_RULES.md를 참고해서 이 규칙대로 작업해줘. [원하는 수정 내용]"
